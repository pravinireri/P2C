# P2C

Turns old code (PowerBuilder, COBOL, VB6) into C# (.NET 8).  
Also grades itself and writes tests.

---

## What it does

- reads legacy code  
- explains it  
- translates it  
- scores the result  
- generates tests  